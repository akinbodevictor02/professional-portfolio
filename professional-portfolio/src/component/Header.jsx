import React from "react";

const Header = () => {
    return (
        <header style={{ padding: "20px", backgroundColor: "#222", color: "#fff"}}>
            <h1>Victor's Portfolio</h1>
        </header>
    );
};

export default Header;